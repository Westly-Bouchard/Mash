// Program to print the current working directory to the screen on unix machines

// Command to run
string cmd = "pwd";

// Variable to store the result of the command
string result;

// Mash library call
exec(cmd, result);

// Echo the command output to the terminal
echo(result);